import type { Metadata } from "next";
export const metadata: Metadata = { title: "Dominion Outreach — Case Study" };
export default function CaseStudyLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}

